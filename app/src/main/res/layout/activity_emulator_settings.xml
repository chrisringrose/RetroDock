<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="24dp"
    android:background="#1a1a2e">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <!-- Header -->
        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Emulator Profiles"
            android:textSize="24sp"
            android:textColor="#e0e0e0"
            android:textStyle="bold"
            android:layout_marginBottom="4dp" />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Swap emulator config folders when docking/undocking"
            android:textSize="13sp"
            android:textColor="#888888"
            android:layout_marginBottom="20dp" />

        <!-- Master Toggle -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:layout_marginBottom="4dp">

            <TextView
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="Enable profile switching"
                android:textSize="16sp"
                android:textColor="#e0e0e0" />

            <Switch
                android:id="@+id/master_toggle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content" />

        </LinearLayout>

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="When enabled, config folders are renamed on dock/undock so each emulator loads different settings."
            android:textSize="12sp"
            android:textColor="#666666"
            android:layout_marginBottom="20dp" />

        <!-- Divider -->
        <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:background="#333355"
            android:layout_marginBottom="16dp" />

        <!-- Emulator list container - rows added dynamically -->
        <LinearLayout
            android:id="@+id/emulator_list"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical" />

        <!-- Help text -->
        <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:background="#333355"
            android:layout_marginTop="8dp"
            android:layout_marginBottom="16dp" />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="How it works:\n1. Enable an emulator and verify its config path\n2. Dock your device \u2014 current settings are saved as handheld\n3. Configure the emulator for docked mode\n4. Future dock/undock events will swap between the two profiles\n\nIf an emulator is running during a swap, you\u2019ll get a notification to restart it."
            android:textSize="11sp"
            android:textColor="#555555"
            android:layout_marginBottom="8dp" />

    </LinearLayout>
</ScrollView>
